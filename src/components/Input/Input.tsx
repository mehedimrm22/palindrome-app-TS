const Input = () => {
  return (
    <div className="palindrome-input">
      <input type="text" placeholder="Enter a word to " />
      <button>Check</button>
    </div>
  );
};

export default Input;
